name: Testing GitHub actions for Application Security Testing Scans

on:
  workflow_dispatch:
  push:
    branches: ["master"]

jobs:
  runs-on: ubuntu-latest
  permissions:
    actions: read
    contents: read
    security-events: write

steps:
  -name: Checkout source code
  uses: actions/checkout@v4
  -name: Run Echo Command
  run: echo "Testing manually written workflow"
